# Root Account Best Practices

Root account is created by default and it should not be used as shared.

## Best Practices

- Enable MFA
- Never use root account daily
- Never create access keys for root
- Use IAM users and roles instead
