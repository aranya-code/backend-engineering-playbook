# AWS CLI Examples

Create Access Point

aws s3control create-access-point --account-id ACCOUNT_ID --name analytics-ap --bucket data-lake

List Access Points

aws s3control list-access-points --account-id ACCOUNT_ID
