# Delete Marker Replication

## What is a Delete Marker?
Created when an object is deleted in a versioned bucket.

## Replication Behavior
Delete marker replication is optional.

## Why It Matters
Helps maintain consistency between source and destination buckets.

## Exam Tip
Object replication and delete marker replication are separate configurations.
