# Docker Compose

---

### Starts services from a Compose file.

```bash
docker compose -f docker-compose.yml up -d
```

### Stops and removes Compose resources.

```bash
docker compose -f docker-compose.yml down
```
