"""User models for the Django MongoDB application.

MongoDB is accessed through the repository/service layer rather than Django's
relational ORM. This module intentionally does not define a Django model.
"""

"""User models for the Django MongoDB application.

MongoDB is accessed through the repository/service layer rather than Django's
relational ORM. This module intentionally does not define a Django model.
"""