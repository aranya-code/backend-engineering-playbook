"""Migration package for the users application.

MongoDB user documents are managed through PyMongo rather than Django's
relational ORM, so no Django model migrations are required for this app.
"""

"""Migration package for the users application.

MongoDB user documents are managed through PyMongo rather than Django's
relational ORM, so no Django model migrations are required for this app.
"""